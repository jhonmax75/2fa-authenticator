# Copy to keystore.properties and replace every placeholder locally.
# Never commit keystore.properties or the referenced .jks file.
KEYSTORE_FILE=my-release-key.jks
KEYSTORE_PASSWORD=replace-with-keystore-password
KEY_ALIAS=my-key
KEY_PASSWORD=replace-with-key-password